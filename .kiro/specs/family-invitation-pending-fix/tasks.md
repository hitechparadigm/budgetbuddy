# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Invalid begins_with on GSI4 Partition Key
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to primary users with pending invitations — the concrete failing case is any call to `handleGetInvitations(familyId, "primary")` where the family has pending invitations stored in DynamoDB
  - Add test to `backend/functions/family/family.pbt.test.js` in a new describe block
  - Mock DynamoDB `ScanCommand` to return pending invitations for the target familyId (this is the expected correct query pattern)
  - Property: for all primary users with familyId that has pending invitations, `handleGetInvitations` should return statusCode 200 with non-empty invitations array containing correct fields (invitationId, email, role, status, createdAt, expiresAt)
  - Use `fc.uuid()` for familyId, `fc.array(fc.record({...}))` for invitation data
  - The bug condition from design: `isBugCondition(input) WHERE input.familyRole = "primary" AND familyHasPendingInvitations(input.familyId) AND queryUsesBeginsWith_OnPartitionKey("GSI4PK")`
  - Run test on UNFIXED code — the current code uses `QueryCommand` with `begins_with` on GSI4PK which will not match the `ScanCommand` mock, causing the test to FAIL
  - **EXPECTED OUTCOME**: Test FAILS (this is correct — it proves the bug exists because the handler uses an invalid query pattern)
  - Document counterexamples found (e.g., "handleGetInvitations('family-123', 'primary') returns 500 or empty array instead of pending invitations")
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 2.1_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Primary Access Denied and Empty Family Behavior
  - **IMPORTANT**: Follow observation-first methodology
  - Add tests to `backend/functions/family/family.pbt.test.js` in a new describe block
  - **Observe on UNFIXED code first:**
  - Observe: `handleGetInvitations(familyId, "spouse")` returns 403 on unfixed code
  - Observe: `handleGetInvitations(familyId, "viewer")` returns 403 on unfixed code
  - Observe: `handleGetInvitations(familyId, "arbitrary-role")` returns 403 on unfixed code
  - **Write property-based tests capturing observed behavior:**
  - Property 2a: For all non-primary roles (`fc.constantFrom("spouse", "viewer")` and `fc.string()`), `handleGetInvitations` returns 403 with error message "Only primary user can view invitations"
  - Property 2b: For all primary users whose family has zero pending invitations, `handleGetInvitations` returns 200 with empty invitations array and count 0 (preserves conditional rendering that hides the Pending Invitations section)
  - Use existing mock patterns from `family.pbt.test.js` (mockSend, createUserClaims, createEvent helpers)
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve — role-based access control and empty-state rendering are not affected by the bug)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.5_

- [x] 3. Fix for invalid DynamoDB query in handleGetInvitations and silent frontend error
  - [x] 3.1 Replace invalid GSI4 query with Scan + FilterExpression in handleGetInvitations
    - In `backend/functions/family/index.js`, function `handleGetInvitations`
    - Replace `QueryCommand` with `ScanCommand` from `@aws-sdk/lib-dynamodb`
    - Remove `IndexName: "GSI4"` and `KeyConditionExpression: "begins_with(GSI4PK, :invPrefix)"`
    - Use `ScanCommand` with `FilterExpression: "begins_with(GSI4PK, :invPrefix) AND familyId = :familyId AND #status = :status"`
    - Add `ExpressionAttributeNames: { "#status": "status" }` (status is a DynamoDB reserved word)
    - Add `ExpressionAttributeValues: { ":invPrefix": "INVITATION#", ":familyId": familyId, ":status": "pending" }`
    - This filters for items where GSI4PK starts with "INVITATION#", matching the target familyId, with status "pending"
    - Ensure `ScanCommand` is imported at the top of the file if not already
    - _Bug_Condition: isBugCondition(input) WHERE queryUsesBeginsWith_OnPartitionKey("GSI4PK") — replaced with Scan + FilterExpression_
    - _Expected_Behavior: handleGetInvitations returns 200 with all pending invitations for the familyId_
    - _Preservation: Non-primary role 403 check occurs before the query, so it is unaffected. Empty results still return empty array._
    - _Requirements: 1.1, 2.1, 2.2, 2.3_

  - [x] 3.2 Add error visibility in frontend FamilySettings.tsx for invitation loading failures
    - In `packages/web-app/src/components/FamilySettings.tsx`, function `loadFamilyMembers`
    - In the inner try/catch block for invitations fetch (around line ~97), instead of silently catching:
    - Add a new state variable `invitationError` (or reuse `error` with a non-blocking warning)
    - When `invitationsResponse.ok` is false, parse the error body and set a warning message
    - When the catch block fires, set a warning message like "Could not load pending invitations. Please try again."
    - Display the invitation error as a yellow/amber warning banner (non-blocking — don't prevent the rest of the page from loading)
    - _Bug_Condition: Silent error swallowing hides API failures from the user_
    - _Expected_Behavior: User sees a warning when invitations fail to load_
    - _Preservation: Page still loads family members even if invitations fail. Existing error/success banners unchanged._
    - _Requirements: 1.2, 2.2_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Invalid begins_with on GSI4 Partition Key
    - **IMPORTANT**: Re-run the SAME test from task 1 — do NOT write a new test
    - The test from task 1 encodes the expected behavior (200 with correct invitations)
    - After the fix, `handleGetInvitations` uses `ScanCommand` which matches the mock setup
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed — handleGetInvitations now returns pending invitations correctly)
    - _Requirements: 2.1, 2.2_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Primary Access Denied and Empty Family Behavior
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions — role-based access control and empty-state behavior unchanged)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Update documentation
  - Update `backend/functions/family/README.md` to document the Scan-based query pattern for `handleGetInvitations`
  - Note the DynamoDB constraint: `begins_with` is only valid on sort keys, not partition keys
  - Document the frontend error visibility improvement in FamilySettings

- [x] 5. Checkpoint - Ensure all tests pass
  - Run full family function test suite: `cd backend/functions/family && npx jest --run`
  - Ensure all property-based tests pass (Property 1 bug condition, Property 2 preservation, and existing Properties 1-5)
  - Ensure existing unit tests in `index.test.js` and `invitation-management.test.js` still pass
  - Ensure no TypeScript errors in `packages/web-app/src/components/FamilySettings.tsx`
  - Ask the user if questions arise
