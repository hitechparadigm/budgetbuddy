{
    "name": "@budget-buddy/api-client",
    "version": "1.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "lint": "eslint src --ext .ts,.tsx",
        "typecheck": "tsc --noEmit",
        "test": "jest",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "aws-amplify": "^6.0.0",
        "swr": "^2.2.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "jest": "^29.0.0",
        "typescript": "^5.1.0"
    }
}