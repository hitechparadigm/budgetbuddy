{
    "name": "@budget-buddy/shared",
    "version": "1.0.0",
    "private": true,
    "description": "Shared components and utilities for BudgetBuddy",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "lint": "eslint src --ext .ts,.tsx",
        "typecheck": "tsc --noEmit",
        "test": "jest",
        "test:unit": "jest",
        "test:coverage": "jest --coverage",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@babel/runtime": "^7.28.4",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "eslint": "^8.45.0",
        "jest": "^29.0.0",
        "ts-jest": "^29.4.6",
        "typescript": "^5.1.0"
    }
}
