{
    "_comment": "Authentication Lambda function package.json - Handles user registration, login, and profile management",
    "name": "budgetbuddy-auth-function",
    "version": "1.0.0",
    "description": "BudgetBuddy authentication Lambda function for user management and Cognito integration",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "local": "sam local start-api"
    },
    "dependencies": {
        "@aws-sdk/client-cognito-identity-provider": "^3.450.0"
    },
    "devDependencies": {
        "jest": "^29.0.0"
    }
}